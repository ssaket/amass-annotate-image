import React, {useEffect} from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

const AnnotateImageFilter = () => {
    useEffect(() => {
        console.log("hi");
    }, []);

    const name = "Image 1";
    const src = "hjjdjdjd";

    return (
        <React.Fragment>
        </React.Fragment>
    );
}

export default AnnotateImageFilter